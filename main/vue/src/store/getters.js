let getters={
    // bFoot:(state)=>state.bFoot,
    bFoot:(state)=>state.bFoot,
    user:(state)=>state.user,
    home:(state)=>state.home,
    // datail:(state)=>state.detail,
    bLoading:(state)=>state.bLoading,
}
export default getters;